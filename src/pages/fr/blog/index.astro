---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import PageHero from '../../../components/PageHero.astro';
import BlogCard from '../../../components/BlogCard.astro';
import { t } from '../../../i18n/utils';
import { publishedPosts } from '../../../data/blog';

const lang = 'fr';
const tr = t(lang);
const blog = tr.blog_page;

const [featured, ...rest] = publishedPosts;
---

<BaseLayout
  lang={lang}
  title={blog.title}
  description="Blog de Banconlé AKOBI : cybersécurité, développement web, cloud, carrière."
>
  <PageHero eyebrow={blog.subtitle} title={blog.title} signature="blog" />

  <div class="om-section">
    <div class="om-blog-grid gsap-stagger">
      {featured && (
        <BlogCard
          title={featured.title}
          category={featured.category}
          date={featured.date}
          excerpt={featured.excerpt}
          slug={featured.slug}
          lang={lang}
          featured={true}
        />
      )}
      {rest.map((post) => (
        <BlogCard
          title={post.title}
          category={post.category}
          date={post.date}
          excerpt={post.excerpt}
          slug={post.slug}
          lang={lang}
        />
      ))}
    </div>
  </div>
</BaseLayout>

<style>
  .om-blog-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(min(100%, 20rem), 1fr));
    gap: clamp(0.875rem, 2vw, 1.5rem);
  }
</style>
